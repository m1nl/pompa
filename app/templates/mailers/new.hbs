{{#bootstrap-modal title="New mailer" actionTitle="Create" cancel=(action "cancel") action=(action "save") close=(route-action "back")}}
  {{#mailer-form model=model}}
  {{/mailer-form}}
{{/bootstrap-modal}}

